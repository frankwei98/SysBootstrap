# Fresh ARM64 VM Validation

Date: 2026-06-08

Environment:

- Debian GNU/Linux 13 (trixie)
- `linux/arm64`
- fresh VM state before validation
- SSH entrypoint: `ssh frankwei@192.168.31.198 -p 2222`

Binary under test:

- local worktree built with `GOOS=linux GOARCH=arm64 go build ./cmd/sys-bootstrap/`

Local verification before remote run:

- `go test ./...`
- `GOOS=linux GOARCH=arm64 go build -o /tmp/sys-bootstrap-arm64 ./cmd/sys-bootstrap/`

Validated commands and outcomes:

- `sys-bootstrap version`: passed
- `sys-bootstrap doctor`: passed
- `sys-bootstrap plan`: passed
- `sys-bootstrap module timezone`: passed, timezone changed from `Asia/Singapore` to `Etc/UTC`
- `sys-bootstrap module fail2ban`: passed
- `sys-bootstrap module base`: passed
- `sys-bootstrap module docker`: passed
- `sys-bootstrap module node`: passed
- `sys-bootstrap module ai`: passed
- `sys-bootstrap module ssh_keygen`: passed
- `sys-bootstrap module user`: passed, created `vmtestuser`
- `sys-bootstrap module ssh`: passed, SSH port changed from `2222` to `22333`

Post-run checks:

- `fail2ban` service: `enabled` and `active`
- `docker` service: `enabled` and `active`
- `frankwei` added to `docker` group
- `vmtestuser` created with `/bin/zsh`
- `vmtestuser` added to `sudo`
- `/etc/sudoers.d/sys-bootstrap-vmtestuser` created for passwordless sudo
- `vmtestuser` received the requested SSH public key
- new SSH port `22333` reachable
- old SSH port `2222` closed
- fail2ban `sshd` jail synced to `port = 22333`

Tool versions confirmed on the VM:

- `node v24.16.0`
- `pnpm 11.5.2`
- `bun 1.3.14`
- `claude 2.1.168`
- `codex-cli 0.137.0`

Observed product semantics:

- After the SSH module changes the machine to `22333`, `sys-bootstrap plan` still shows `SSH Hardening` as pending because the preview default target remains `22122`.
- `fail2ban` correctly follows the actual system SSH port when no explicit target port is selected.

Conclusion:

- The current worktree passed fresh-machine validation on Debian 13 ARM64 across root modules, user modules, and the interactive SSH/user flows.
